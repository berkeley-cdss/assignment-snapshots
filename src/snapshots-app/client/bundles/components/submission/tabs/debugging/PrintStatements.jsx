import React from 'react';
import { Typography } from '@mui/material';

const PrintStatements = () => {
  return <Typography variant="h4">Print Statements</Typography>;
};

export default PrintStatements;